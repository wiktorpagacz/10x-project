# E2E Test Environment Configuration
# Copy this file to .env.test and fill in your values

# Test User Credentials (must exist in Supabase)
E2E_USERNAME=your-test-user@example.com
E2E_PASSWORD=your-test-password
E2E_USERNAME_ID=user-uuid-from-supabase

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-public-key

# Notes:
# 1. Create a test user in Supabase Dashboard → Authentication → Users
# 2. Make sure the user's email is confirmed
# 3. Copy the User ID from the Users table
# 4. Get SUPABASE_URL and SUPABASE_KEY from Settings → API
# 5. NEVER commit .env.test to git!
